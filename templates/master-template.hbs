    <div id="wrapper">



        <!-- Page Content -->
<!-- Page Content -->

  <div id="page-content-wrapper">
        <nav id="nav2" class="navbar navbar-default">
          <div class="container-fluid">
            {{!-- <!-- Brand and toggle get grouped for better mobile display --> --}}
            <div class="navbar-header">
              <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </button>
            </div>
            {{!-- <!-- Collect the nav links, forms, and other content for toggling --> --}}
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

              <ul class="col-md-2 nav navbar-nav"></ul>
              <ul class="col-md-1 nav navbar-nav"> {{!-- Rides Dropdown --}}
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle ride" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Rides</a>
                  <ul class="dropdown-menu">
                      {{#each attraction}}
                      {{#if (eq type_id 1)}}
                    <li class="potato" value ="{{id}}"><a data-toggle="modal" data-target="#myModal" href="#">
                        {{name}}
                    </a></li>
                      {{/if}}
                      {{/each}}
                  </ul>
                </li>
              </ul>






              <ul class="col-md-1 nav navbar-nav"> {{!-- Restaurants Dropdown --}}
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Restaurants</a>
                  <ul class="dropdown-menu">
                      {{#each attraction}}
                      {{#if (eq type_id 2)}}
                    <li  class="potato" value ="{{id}}"><a data-toggle="modal" data-target="#myModal" href="#">
                        {{name}}
                    </a></li>
                      {{/if}}
                      {{/each}}
                  </ul>
                </li>
              </ul>


              <ul class="col-md-1 nav navbar-nav"> {{!-- Shows Dropdown --}}
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Shows</a>
                  <ul class="dropdown-menu">
                      {{#each attraction}}
                      {{#if (eq type_id 3)}}
                    <li class="potato" value ="{{id}}"><a data-toggle="modal" data-target="#myModal" href="#">
                        {{name}}
                    </a></li>
                      {{/if}}
                      {{/each}}
                  </ul>
                </li>
              </ul>




              <ul class="col-md-1 nav navbar-nav"> {{!-- Vendor Dropdown --}}
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Vendors</a>
                  <ul class="dropdown-menu">
                      {{#each attraction}}
                      {{#if (eq type_id 4)}}
                    <li class="potato" value ="{{id}}"><a data-toggle="modal" data-target="#myModal" href="#">
                        {{name}}
                    </a></li>
                      {{/if}}
                      {{/each}}
                  </ul>
                </li>
              </ul>


              <ul class="col-md-1 nav navbar-nav"> {{!-- Characters Dropdown --}}
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Characters</a>
                  <ul class="dropdown-menu">
                      {{#each attraction}}
                      {{#if (eq type_id 5)}}
                    <li class="potato" value ="{{id}}"><a data-toggle="modal" data-target="#myModal" href="#">
                        {{name}}
                    </a></li>
                      {{/if}}
                      {{/each}}
                  </ul>
                </li>
              </ul>

              <ul class="col-md-1 nav navbar-nav"> {{!-- Animals Dropdown --}}
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Animals</a>
                  <ul class="dropdown-menu">
                      {{#each attraction}}
                      {{#if (eq type_id 6)}}
                    <li class="potato" value ="{{id}}"><a data-toggle="modal" data-target="#myModal" href="#">
                        {{name}}
                    </a></li>
                      {{/if}}
                      {{/each}}
                  </ul>
                </li>
              </ul>

              <ul class="col-md-1 nav navbar-nav"> {{!-- Games Dropdown --}}
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Games</a>
                  <ul class="dropdown-menu">
                      {{#each attraction}}
                      {{#if (eq type_id 7)}}
                    <li class="potato" value ="{{id}}"><a data-toggle="modal" data-target="#myModal" href="#">
                        {{name}}
                    </a></li>
                      {{/if}}
                      {{/each}}
                  </ul>
                </li>
              </ul>

              <ul class="col-md-2 nav navbar-nav"> {{!-- Special Events Dropdown --}}
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Special Events</a>
                  <ul class="dropdown-menu">
                      {{#each attraction}}
                      {{#if (eq type_id 8)}}
                    <li class="potato" value ="{{id}}"><a data-toggle="modal" data-target="#myModal" href="#">
                        {{name}}
                    </a></li>
                      {{/if}}
                      {{/each}}
                  </ul>
                </li>
              </ul>


            </div>{{!-- <!-- /.navbar-collapse --> --}}
          </div>{{!-- <!-- /.container-fluid --> --}}
        </nav>


    </div>
    <!-- /#wrapper -->




