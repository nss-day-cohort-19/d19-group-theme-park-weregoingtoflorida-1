<div id="wrapper">
  <!-- Sidebar -->
  <div id="sidebar-wrapper">
      <ul class="sidebar-nav">
          <li class="sidebar-brand">
              <a href="#">
                  Start Bootstrap
              </a>
          </li>
          <li>
              <a href="#">Dashboard</a>
          </li>
          <li>
              <a href="#">Shortcuts</a>
          </li>
          <li>
              <a href="#">Overview</a>
          </li>
          <li>
              <a href="#">Events</a>
          </li>
          <li>
              <a href="#">About</a>
          </li>
          <li>
              <a href="#">Services</a>
          </li>
          <li>
              <a href="#">Contact</a>
          </li>
      </ul>
  </div>
  <!-- /#sidebar-wrapper -->
  <!-- Page Content -->
  <div id="page-content-wrapper">
        <div class ="navbar navbar-default">
        <h1>Magik Kingdum</h1>
        <a href="#" class="btn btn-default" id="menu-toggle">Toggle Menu</a>
    </div> {{!-- end navbar wrapper --}}
        <nav class="navbar navbar-default">
          <div class="container-fluid">
            {{!-- <!-- Brand and toggle get grouped for better mobile display --> --}}
            <div class="navbar-header">
              <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </button>
              <a class="navbar-brand" href="#">Brand</a>
            </div>
            {{!-- <!-- Collect the nav links, forms, and other content for toggling --> --}}
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">


              <ul class="nav navbar-nav"> {{!-- Rides Dropdown --}}
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Rides <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                      {{#each attraction}}
                      {{#if (eq type_id 1)}}
                    <li><a href="#">
                        {{name}}
                    </a></li>
                      {{/if}}
                      {{/each}}
                  </ul>
                </li>
              </ul>


              <ul class="nav navbar-nav"> {{!-- Restaurants Dropdown --}}
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Restaurants <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                      {{#each attraction}}
                      {{#if (eq type_id 2)}}
                    <li><a href="#">
                        {{name}}
                    </a></li>
                      {{/if}}
                      {{/each}}
                  </ul>
                </li>
              </ul>


              <ul class="nav navbar-nav"> {{!-- Shows Dropdown --}}
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Shows <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                      {{#each attraction}}
                      {{#if (eq type_id 3)}}
                    <li><a href="#">
                        {{name}}
                    </a></li>
                      {{/if}}
                      {{/each}}
                  </ul>
                </li>
              </ul>




              <ul class="nav navbar-nav"> {{!-- Vendor Dropdown --}}
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Vendors <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                      {{#each attraction}}
                      {{#if (eq type_id 4)}}
                    <li><a href="#">
                        {{name}}
                    </a></li>
                      {{/if}}
                      {{/each}}
                  </ul>
                </li>
              </ul>


              <ul class="nav navbar-nav"> {{!-- Characters Dropdown --}}
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Characters <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                      {{#each attraction}}
                      {{#if (eq type_id 5)}}
                    <li><a href="#">
                        {{name}}
                    </a></li>
                      {{/if}}
                      {{/each}}
                  </ul>
                </li>
              </ul>

              <ul class="nav navbar-nav"> {{!-- Animals Dropdown --}}
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Animals <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                      {{#each attraction}}
                      {{#if (eq type_id 6)}}
                    <li><a href="#">
                        {{name}}
                    </a></li>
                      {{/if}}
                      {{/each}}
                  </ul>
                </li>
              </ul>

              <ul class="nav navbar-nav"> {{!-- Games Dropdown --}}
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Games <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                      {{#each attraction}}
                      {{#if (eq type_id 7)}}
                    <li><a href="#">
                        {{name}}
                    </a></li>
                      {{/if}}
                      {{/each}}
                  </ul>
                </li>
              </ul>

              <ul class="nav navbar-nav"> {{!-- Special Events Dropdown --}}
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Special Events <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                      {{#each attraction}}
                      {{#if (eq type_id 8)}}
                    <li><a href="#">
                        {{name}}
                    </a></li>
                      {{/if}}
                      {{/each}}
                  </ul>
                </li>
              </ul>


            </div>{{!-- <!-- /.navbar-collapse --> --}}
          </div>{{!-- <!-- /.container-fluid --> --}}
        </nav>
    </div><!-- /#page-content-wrapper -->
</div><!-- /#wrapper -->















